steps:
- name: 'gcr.io/cloud-builders/docker'
  script: |
    docker build -t us-west2-docker.pkg.dev/dusty-chat-agent/quickstart-docker-repo/quickstart-image:${_IMAGE_TAG} .
  automapSubstitutions: true
images:
- 'us-west2-docker.pkg.dev/$PROJECT_ID/quickstart-docker-repo/quickstart-image:${_IMAGE_TAG}'